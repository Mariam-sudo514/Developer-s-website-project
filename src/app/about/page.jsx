import { AboutPageContent } from '@/components/InfoPages/InfoPages';

export const metadata = {
	title: 'About me | Maria.dev',
	description:
		'Thoughtful design, clean code, and human-centered web experiences by Maria.dev.',
};

const AboutPage = () => {
	return <AboutPageContent />;
}

export default AboutPage;
